#include<bits/stdc++.h>
using namespace std;
int main()
{
	long long int b,n;
	cin>>b>>n;
	long long int wt[b]
	for(long long int i=0;i<b;i++)
	cin>>wt[i];
	sort(wt,wt+b);
	for(long long int i=0;i<n;i++)
	cin>>wtp[i];
	sort(wtp,wtp+n);
	
	return 0;
}
